export enum MeetingTypes {
  Postponed = "postponed",
  Cancelled = "cancelled",
  Scheduled = "scheduled",
  None = "None"
}

export enum ButtonType {
  Student = "student",
  Patient = "patient",
  Admin = "admin",
  Request = "request",
  Schedule = "schedule"
}
