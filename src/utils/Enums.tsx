export enum MeetingTypes {
  Postponed = "postponed",
  Cancelled = "cancelled",
  Scheduled = "scheduled",
}

export enum ButtonType {
  Student = "student",
  Patient = "patient",
  Admin = "admin",
}
